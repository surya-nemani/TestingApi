name: manual testing from dev
on:
  workflow_dispatch:
  
jobs:
  build_code:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build triggered manually..."
      
      - name: Exit if not on master branch
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Deploying to production only from main branch.  Current branch is: $GITHUB_REF"
          # exit 1
          
